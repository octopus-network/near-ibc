[package]
name = "escrow-factory"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
near-sdk = "4.1.0"
ibc = { version = "0.41", default-features = false, features = ["borsh", "serde"]}
hex = "0.4"
utils = { path = "../utils" }
