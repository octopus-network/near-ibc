[package]
name = "escrow-factory"
version = "1.0.1"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
near-sdk = { workspace = true }
ibc = { workspace = true }
hex = { workspace = true }
utils = { path = "../utils" }
